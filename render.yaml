services:
  - type: web
    name: flask-discord-ticket
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DISCORD_BOT_TOKEN
        value: your_bot_token
      - key: DISCORD_CHANNEL_ID
        value: your_channel_id
